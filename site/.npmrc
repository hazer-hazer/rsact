# The site's dependencies are all public packages. Pin the public npm registry
# explicitly so installs (local dev and CI) never depend on a contributor's
# machine-global registry (e.g. a corporate mirror) — the committed lockfile
# must resolve for anyone cloning this public repo.
registry=https://registry.npmjs.org/
